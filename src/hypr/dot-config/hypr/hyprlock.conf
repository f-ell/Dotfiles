general {
    hide_cursor = true
    ignore_empty_input = true
    fail_timeout = 3000
}

animations {
    animation = fadeIn, 1, 4, linear
    animation = fadeOut, 1, 2, linear
    animation = inputFieldDots, 1, 0.1
}

background {
    monitor =
    color = rgb(2a3439)

    path = ~/.wallpaper
    blur_passes = 1
    blur_size = 1
    noise = 0.02
}

background {
    monitor = DP-2

    path = ~/.wallpaper-lock
    blur_passes = 1
    blur_size = 1
    noise = 0.02
}

# Header
label {
    monitor = DP-2

    text = monarch
    color = rgb(a7c080)
    font_size = 56
    font_family = Ellograph CF

    position = -640, 384
    halign = center
    valign = bottom
}

# Time
label {
    monitor = DP-2

    text = cmd[update:1000] printf "`date +'%I:%M'`<span size=\"xx-small\">`date +' %p'`</span>"
    color = rgb(a7c080)
    font_size = 24
    font_family = Ellograph CF

    position = -640, 328
    halign = center
    valign = bottom
}

# Date
label {
    monitor = DP-2

    text = cmd[update:1000] printf "`date +'%a. %d %B'`"
    color = rgb(a7c080)
    font_size = 16
    font_family = Ellograph CF

    position = -640, 304
    halign = center
    valign = bottom
}

# User
shape {
    monitor = DP-2

    size = 288, 56
    color = rgba(a8aeb140)
    shadow_passes = 2

    border_size = 2
    border_color = rgba(a8aeb160)
    rounding = 4

    position = -640, 192
    halign = center
    valign = bottom
}
label {
    monitor = DP-2

    text =  $USER
    color = rgb(a8aeb1)
    font_size = 16
    font_family = Ellograph CF

    position = -640, 210 # 192 + 56/2 - 16/2 - 2
    halign = center
    valign = bottom
}


# Input
input-field {
    monitor = DP-2
    size = 288, 56
    shadow_passes = 2

    dots_center = true
    dots_size = 0.2
    hide_input = false
    fade_on_empty = false

    font_family = Ellograph CF
    placeholder_text =  Password
    fail_text = Error

    font_color = rgb(a8aeb1)
    inner_color = rgba(a8aeb140)
    check_color = rgb(dfa000)
    fail_color = rgb(f85552)

    outline_thickness = 2
    outer_color = rgba(a8aeb160)
    rounding = 4

    position = -640, 104
    halign = center
    valign = bottom
}
